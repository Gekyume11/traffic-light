const redLight = document.getElementById("red"), yellowLight = document.getElementById("yellow"), greenLight = document.getElementById("green"), timer = document.getElementById("timer"); let state = "red", time = 5; const updateTrafficLight = () => { redLight.classList.remove("active"), yellowLight.classList.remove("active"), greenLight.classList.remove("active"), "red" == state ? (redLight.classList.add("active"), state = "green") : "green" == state ? (greenLight.classList.add("active"), state = "yellow") : "yellow" == state && (yellowLight.classList.add("active"), state = "red") }, trafficLight = () => { time-- <= 0 ? (time = 5, updateTrafficLight()) : timer.innerHTML = `${time}` }; setInterval(trafficLight, 1e3);